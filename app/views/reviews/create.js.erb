$('#new_review').each(function(){
    this.reset();
});
$("#new_review").fadeIn();

$("#reviews_feed").prepend("<%= escape_javascript(render(@review)) %>");

$("#reviews_feed li:first").hide();
$("#reviews_feed li:first").show("slow");


$("html, body").animate({ scrollTop: 0 }, "fast");